---
import Base from '@layouts/Base.astro';

const path = '/the-page-you-wanted';
const today = '2026-04-22';
---

<Base title="404 (man) — Taylor Silenzio">
  <div class="mx-auto max-w-3xl px-6 pt-20 pb-20">
    <pre class="font-mono text-sm leading-6 overflow-x-auto whitespace-pre text-(--color-text)">NOTFOUND(4)                  Site Manual                  NOTFOUND(4)

<span class="font-semibold">NAME</span>
       {path} - page that does not exist

<span class="font-semibold">SYNOPSIS</span>
       GET {path}

<span class="font-semibold">DESCRIPTION</span>
       The requested URL does not correspond to any resource on
       this site. It may have been moved, renamed, or never
       written in the first place.

<span class="font-semibold">EXIT STATUS</span>
       404    Not Found

<span class="font-semibold">SEE ALSO</span>
       <a href="/" class="hover:underline">home(1)</a>, <a href="/blog" class="hover:underline">blog(1)</a>, <a href="/#projects" class="hover:underline">projects(1)</a>

tsilenz.io                    {today}                  NOTFOUND(4)</pre>
  </div>
</Base>
